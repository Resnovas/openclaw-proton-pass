{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "onepassword-contract",
  "projectType": "library",
  "sourceRoot": "libs/onepassword-contract/src",
  "tags": [
    "type:domain",
    "scope:shared",
    "type_core",
    "layer_shared"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -b libs/onepassword-contract/tsconfig.json"
      }
    },
    "check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -b libs/onepassword-contract/tsconfig.json --noEmit false"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run tests/libs/onepassword-contract --passWithNoTests",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
