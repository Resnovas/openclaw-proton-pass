---
title: "API reference"
description: "Generated from the JSDoc on every exported symbol, with every example type-checked and executed."
---

These pages are generated by [`@effect/docgen`](https://github.com/Effect-TS/effect/tree/main/packages/tools/docgen)
from the source. Every `@example` on this site is compiled and run as part of
`pnpm verify`, so an example that stopped being true would fail the build
rather than sit here misleading you.

Edit the JSDoc in `libs/*/src` or `apps/*/src` and run `pnpm docs:api` to
regenerate.

## Libraries

The reusable contracts. Each is an internal workspace package.

<Columns cols={2}>
  <Card title="Domain" href="/reference/api/domain/index">
    The vocabulary: secret ids, vault references, routes, the wire protocol and the error union.
  </Card>
  <Card title="Config" href="/reference/api/config/index">
    Every filesystem path and environment variable this system reads, resolved in one place.
  </Card>
  <Card title="Pass CLI" href="/reference/api/pass-cli/index">
    The two services that talk to Proton Pass: session bootstrap and secret resolution.
  </Card>
  <Card title="Telemetry" href="/reference/api/telemetry/index">
    The closed event union and the filters that make a secret structurally untransmittable.
  </Card>
</Columns>

## Executables

The four binaries. Their entry points are excluded, because importing one would
start the program; they are covered by tests that run the built binaries end to
end.

<Columns cols={2}>
  <Card title="resolver" href="/reference/api/resolver/index">
    The exec secret provider: one JSON request on stdin, one response on stdout.
  </Card>
  <Card title="pass-run" href="/reference/api/pass-run/index">
    The stdio MCP wrapper, which resolves environment references as it launches the child.
  </Card>
  <Card title="mcp-auth-proxy" href="/reference/api/mcp-auth-proxy/index">
    The loopback hop: route matching, header rewriting, forwarding and relay.
  </Card>
  <Card title="cli" href="/reference/api/cli/index">
    setup and doctor, and the layer that supplies them.
  </Card>
</Columns>
